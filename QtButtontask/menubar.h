#ifndef MENUBAR_H
#define MENUBAR_H
// #include <QEvent>
// #include <QPaintEvent>
#include <QMenuBar>

class MenuBar : public QMenuBar
{
public:
    MenuBar();
    ~MenuBar();
    // bool event(QEvent *e)override;
    // void paintEvent(QPaintEvent *event)override;
};

#endif // MENUBAR_H
